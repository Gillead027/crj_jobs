// Este arquivo configura o Next.js, que e o framework usado para criar o site.
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Esta opcao deixa a configuracao pronta para receber ajustes no futuro.
  reactStrictMode: true,
};

// Esta linha entrega a configuracao para o Next.js usar quando o projeto roda.
export default nextConfig;
